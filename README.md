1. Download go2rtc, place index.html file in www folder inside go2rtc folder created. (main)
2. Download cloudflare and run it on a terminal
3. robot_controller.py runs on a terminal (main)
4. go2rtc.yaml runs on a terminal. (main)

Other files are from previous iterations
